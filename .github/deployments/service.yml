apiVersion: v1
kind: Service
metadata:
  name: moj-components-docs-service
spec:
  selector:
    app: moj-components-docs # this should match the pod label in deployment.yml
  ports:
    - name: https
      port: 80
      targetPort: 80
