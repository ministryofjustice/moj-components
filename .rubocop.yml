---
inherit_gem:
  rubocop-govuk:
    - config/default.yml
    - config/rspec.yml

AllCops:
  NewCops: enable
  TargetRubyVersion: 3.3
plugins:
  - rubocop-rspec
  - rubocop-capybara
  - rubocop-rake
